Input is not sanitized to any degree of satisfaction, give the prompt only what it wants!

New game:
  random 5-letter word is pulled from list of words (from 5letterwords.txt)
  expected input: 5-letter word in lowercase  
  output will be a 5-character combination of 'X', 'þ', and '/' corresponding to wrong, right, and misplaced letters respectively
  
Word search:
  positions are numbered 0-4, e.g., in "first," the f has position 0 and t has position 4
  instead of using an empty string for "doesn't contain," if so desired, enter a numeral
  
